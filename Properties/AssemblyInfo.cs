using System.Reflection; // Imports reflection functionality, needed to use assembly-level attributes.

// Specifies the title of the assembly, which is used in assembly metadata and by tools (e.g., in file properties).
[assembly: AssemblyTitle("FilesyncPilot")]

// Specifies the company that produced the assembly; this information appears in assembly metadata and file properties.
[assembly: AssemblyCompany("FilesyncPilot")]

// Specifies the product name the assembly is part of; useful for grouping related assemblies.
[assembly: AssemblyProduct("FilesyncPilot")]

// Defines the version of the assembly used by the CLR during assembly binding.
// This version is important for .NET to locate and load the correct version of the assembly.
[assembly: AssemblyVersion("1.0.0.0")]

// Specifies the file version, which is used for informational purposes in file properties (does not affect binding).
[assembly: AssemblyFileVersion("1.0.0.0")]
